# Changelog

## 4.0.8

- Baseado no `brand-config.json` enviado pelo Marcelo.
- Nome do plugin mantido como `Mesh Branding`.
- Custom logos em `meshcentral-data/plugins/mesh_branding`.
- Fallback padrão em `meshcentral-data/Aplicado_Logo.png`.
- Adicionado middleware para substituir `<title>` também na tela de login.
- Mantida interceptação de `/loginlogo.png` e `/logo.png`.
- Mantidos `MainMeshImage`, `Meu Servidor`, `/serverpic.ashx`, background e cores intactos.

## 4.0.7

- Separação entre logos customizados na pasta do plugin e logo padrão no diretório base.
